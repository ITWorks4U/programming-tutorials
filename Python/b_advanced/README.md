#   contains...

0.  randomizer
    -   use the module `random`
    -   pick a random number
    -   play lottery
1.  global expressions
    -   purpose
    -   special pythonic handling
    -   pitfalls
2.  None is not nothing
    -   what the type `None` means and why this exists
    -   What happens by using None?
3.  strings in python
    -   special handling and pitfalls
    -   random password generator
    -   check, if a word is a palindrome
4.  `main` for python
    -   why Python doesn't comes with a required main at the beginning
        -   and often (for simple scripts) doesn't require a main
    -   What happens, if multiple mains exists?
    -   handle arguments for main
5.  write your own modules
    -   split your script into multiple scripts and use your own modules
    -   pitfalls
6.  regular expressions
    -   purpose
    -   how to use them
    -   check, if an input is a truly integer
7.  OOP programming with Python
    -   basics with OOP
    -   inheritance
    -   polymorphism
    -   thunder methods
    -   singleton patterns
    -   python dataclasses
8.  exception handling
    -   What happens, when an error has been spotted?
    -   practical examples
9.  signal handling
    -   What happens, when a sepcific signal has been raised?
    -   difference to exceptions and why a *signal* is **never** identical to an *exception*
    -   dependencies of the used operating system (**OS**)